# Infrastructure

Reserved for runtime resources such as local PostgreSQL, S3-compatible object storage, deployment manifests, and observability configuration. Infrastructure files describe the environment applications run in; application code remains in `backend/`, `web/`, or `ios/`.
