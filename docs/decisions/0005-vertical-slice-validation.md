# 0005: Prove capabilities vertically

Status: accepted

After the Catalog backend contract is stable, validate the same behavior through generated TypeScript and Swift transports and minimal client implementations before expanding into additional backend domains. Product behavior lives in `docs/product/`, not implicitly in one client.
