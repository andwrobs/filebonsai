# Filebonsai iOS

Reserved for the native SwiftUI/TCA application. The intended direction is feature reducers over handwritten dependency clients that wrap the generated Swift transport. Background transfer ownership must remain independent of screen observation.
